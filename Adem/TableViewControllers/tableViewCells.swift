//
//  tableViewCells.swift
//  Adem
//
//  Created by Coleman Coats on 9/27/19.
//  Copyright © 2019 Coleman Coats. All rights reserved.
//

import Foundation
import UIKit
/*
//MARK:
struct settingsCells {
 var settingOption : String
 var settingImageIcon : UIImage
}

class listSwitcher: UITableViewCell {
    
    var product : settingsCells? {
        didSet {
            productImage.image = product?.settingImageIcon
            productNameLabel.text = product?.settingOption
        }
     }
     
     private let productImage : UIImageView = {
     let imgView = UIImageView(image: #imageLiteral(resourceName: “glasses”))
     imgView.contentMode = .scaleAspectFit
     imgView.clipsToBounds = true
     return imgView
     }()
    
     private let productNameLabel : UILabel = {
     let lbl = UILabel()
     lbl.textColor = .black
     lbl.font = UIFont.boldSystemFont(ofSize: 16)
     lbl.textAlignment = .left
     return lbl
     }()
     
     private let increaseButton : UIButton = {
     let btn = UIButton(type: .custom)
     btn.setImage(#imageLiteral(resourceName: “addTb”), for: .normal)
     btn.imageView?.contentMode = .scaleAspectFill
     return btn
     }()
     
     override init(style: UITableViewCell.CellStyle, reuseIdentifier: String?) {
     super.init(style: style, reuseIdentifier: reuseIdentifier)
     addSubview(productImage)
     addSubview(productNameLabel)
     addSubview(increaseButton)
     NSLayoutConstraint.activate([
        
        productImage.leadingAnchor.constraint(equalTo: self.leadingAnchor, constant: 5),
        productImage.widthAnchor.constraint(equalToConstant: 50),
        productNameLabel.leadingAnchor.constraint(equalTo: productImage.trailingAnchor, constant: 5),
        increaseButton.trailingAnchor.constraint(equalTo: self.rightAnchor, constant: 5)
        ])
     
     }
     
     required init?(coder aDecoder: NSCoder) {
        fatalError("init(coder:) has not been implemented”)
     }
    
    
    
    
}
*/
